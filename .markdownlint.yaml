# markdownlint rules – relaxed for cheat sheets
default: true
MD013: false   # line length – commands can be long
MD033: false   # inline HTML allowed (tables, <details>)
MD041: false   # first line need not be H1 (frontmatter)
MD024:
  siblings_only: true   # repeated headings across sections are OK
